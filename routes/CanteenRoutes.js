const express = require('express');
const router = express.Router();
const axios = require('axios');
const CanteenItem = require('../models/canteen_items');

// ✅ Fetch all items


module.exports = router;
